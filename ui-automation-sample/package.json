{
  "name": "ui-automation-sample",
  "version": "1.0.0",
  "private": true,
  "description": "Sample Playwright TypeScript UI automation project for portfolio use.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "@types/node": "^24.3.0"
  }
}
